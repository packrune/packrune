apiVersion: v1
kind: Service
metadata:
  name: {{ include "packrune.fullname" . }}
  labels: {{- include "packrune.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector: {{- include "packrune.selectorLabels" . | nindent 4 }}
